# Crypto Deposit Detection Storage Design

This document defines the next safe step after the BSC dry-run detector helper and admin dry-run UI.

It is a design/checkpoint only. It does not add implementation code, database writes, watcher state updates, wallet crediting, user-facing address exposure, sweeping, signing, private keys, seed phrases, packages, or route changes.

## Current state before storage