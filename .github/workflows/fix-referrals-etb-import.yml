name: Fix referrals ETB import

on:
  push:
    branches:
      - standardize-etb-unit-style

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Add missing referrals import and remove workflow
        run: |
          python <<'PY'
          from pathlib import Path
          path = Path('src/routes/_app/referrals.tsx')
          text = path.read_text()
          if 'CurrencyUnit' not in text.split('export const Route')[0]:
              text = text.replace('import { SectionHeader } from "@/components/ui/SectionHeader.js";\n', 'import { SectionHeader } from "@/components/ui/SectionHeader.js";\nimport { CurrencyUnit } from "@/components/ui/AmountText.js";\n')
          path.write_text(text)
          Path('.github/workflows/fix-referrals-etb-import.yml').unlink(missing_ok=True)
          PY
          git config user.name "qhash-etb-bot"
          git config user.email "qhash-etb-bot@users.noreply.github.com"
          git add src/routes/_app/referrals.tsx .github/workflows/fix-referrals-etb-import.yml
          if git diff --cached --quiet; then
            echo "No referrals import fix needed."
            exit 0
          fi
          git commit -m "Fix referrals ETB unit import"
          git push
